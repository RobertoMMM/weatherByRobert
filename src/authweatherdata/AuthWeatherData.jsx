import React from 'react'
import useStoreData from '../hooks/use-store';

const AuthWeatherData = () => {
  return (
      <>
      </>
    // <div style={{position: 'absolute', top: 0, left: 0, color: 'white'}}>{locations.map((doc)=> (
    //     <p>{doc}</p>
    // ))}</div>
  )
}

export default AuthWeatherData